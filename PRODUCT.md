# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要面向探索求职方向的学生和初入职场者，适用于技术、运营、设计、财务等不同岗位。用户通常先浏览招聘平台，再收集同类岗位并决定如何准备或修改简历。

## Product Purpose

帮助用户从多条零散 JD 中识别反复出现的要求，查看这些结论对应的招聘原文，再按需与自己的简历对照，形成可执行的准备建议。单岗位精读保留给已确定投递目标的用户。

## Positioning

重点不是替代招聘平台检索，也不是为一份 JD 打匹配分，而是比较 2-10 份同类岗位，并按不同岗位计数、保留原文依据。方向画像只代表当前样本，不代表整个行业或录用概率。

## Operating Context

用户可通过 Offer岛接口搜索，或粘贴在 BOSS 等渠道找到的 JD；自动搜索不抓取 BOSS，且目前无法覆盖所有行业。清单可连续添加、勾选、全选、全不选和移除。分析中心提供多岗位方向画像、可选的简历对照，以及单岗位精读。用户可下载可读 TXT 报告。

## Capabilities and Constraints

- 保留当前四条 Vue 路由：`/`、`/find`、`/saved`、`/analysis`，以及搜索、添加、勾选、分析、简历上传和报告下载功能。
- 只有用户明确确认后才调用 DeepSeek；搜索、添加、勾选和本地提取简历文字不调用模型。API Key 留在 Python 服务端。
- 当前没有账号和数据库；岗位清单只留在浏览标签的会话存储中。服务端请求次数限制不能替代 API 平台的消费上限。
- 现有线上网址仍部署 Streamlit 旧版；Ant Design Vue 版本尚未公开部署。
- 扫描版 PDF 不支持；常见联系方式会遮盖，但用户仍需检查姓名、地址等敏感信息。
- 待决定：覆盖更多行业的自动岗位来源、账号与长期保存、正式部署和强配额。

## Brand Commitments

产品名称为“求职对照台”。用户已明确要求本次改版整体以蓝、白为主，保留现有功能与路由。界面应让用户看清样本、来源、频率和操作费用，而不是夸大模型判断。

## Evidence on Hand

已有可运行的 Vue 页面和 Python 分析逻辑、示例 JD、离线测试，以及旧版 Streamlit 网页。没有真实用户评价、录用成效、行业覆盖率或商业指标；设计示例必须明确标注为示例。

## Product Principles

1. 多岗位比较优先，单岗位分析可选。
2. 每条重要结论都能回到 JD 或简历原文核对。
3. 把模型费用与数据去向放在发起请求之前说明。
4. 不把当前样本频率伪装成行业统计或录用预测。
5. 降低连续收集岗位时的页面切换成本。
